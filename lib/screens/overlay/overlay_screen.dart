import 'package:flutter/material.dart';

class OverlayScreen extends StatelessWidget {
  const OverlayScreen({super.key, required bool isToggled});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
